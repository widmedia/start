rather use:
echo '</div>\n<div class="row"><div class="twelve columns"><hr /></div></div>\n';        
than:
echo "</div>\n<div class='row'><div class='twelve columns'><hr /></div></div>\n";        

because php processes echo 'some string' faster than "some string" as the single quote does not replace any $variables (which is a bad thing anyway)

